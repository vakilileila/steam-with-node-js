
<article class="content">
   <!-- <div class="row  containerBg">
        <div class="col-md-4">


            <form name="list hero" target="_blank" method="GET" onsubmit="">
                <label>hero:</label>
                <select class="select-dropdown" name="list hero"></select>

            </form>


        </div>
        <div class="col-md-4">
            <form name="list hero" target="_blank" method="GET" onsubmit="">
                <label>Rarity:</label>
                <select class="select-dropdown" name="list hero">
                    <option value="one">One</option>
                    <option value="two">Two</option>
                </select>

            </form>
        </div>
        <div class="col-md-4">
            <form name="list hero" target="_blank" method="GET" onsubmit="">
                <label>Sort by:</label>
                <select class="select-dropdown" name="list hero">
                    <option value="one">One</option>
                    <option value="two">Two</option>
                </select>

            </form>
        </div>


    </div>-->
    <div class="row">

    </div>
    <div class="containerBg row">

        <% categoreis.forEach(function(category){ %>
        <div class="box-hero">


            <div class="item-hero">
                <a href="/heros/<%= category._id %>">

                    <img src="<%= category.imageUrl %>">

                    <div class="item-info-category">

                        <div class="item-name"><%= category.name %></div>
                    </div>

                </a>


            </div>

        </div>
        <% }); %>

    </div>

</article>


<div id="grid"></div>

<script>
    $(function () {
        $('#grid').kendoGrid({
            dataSource: new kendo.data.DataSource({
                transport: {
                    read: ''
                },
                parameterMap: function (options, parameters) {

                },
                pageSize: 10,
                schema: {
                    model: {
                        id: '_id',
                        fields: {
                            name: {type: "string"}
                        }
                    }
                }
            }),
            navigatable: true,
            pageable: true,
            columns: [
                {field: 'name', title: 'Name'},
            ]
        })
    });

    /*format: "{0:c}*/
</script>